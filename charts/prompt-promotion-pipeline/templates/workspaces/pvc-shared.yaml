---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prompt-promotion-workspace
  annotations:
    volume.beta.kubernetes.io/storage-provisioner: kubernetes.io/aws-ebs
spec:
  resources:
    requests:
      storage: 1Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
