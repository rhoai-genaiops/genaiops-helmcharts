---
kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: prompt-promotion-event-listener
spec:
  path: /
  to:
    kind: Service
    name: el-prompt-promotion-event-listener
    weight: 100
  port:
    targetPort: http-listener
  tls:
    termination: edge
  wildcardPolicy: None
